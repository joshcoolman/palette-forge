node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vercel
.vinxi
__unconfig*
todos.json

# Local dev eval capture (the capability is committed; the runs are local data)
eval/runs.jsonl
eval/latest.json
