# Copy this to .env.local and fill in your key.
# .env.local is git-ignored (covered by *.local in .gitignore).

ANTHROPIC_API_KEY=sk-ant-...

# Optional: haiku (default, fast + cheap for iteration) or sonnet (quality check)
# ANTHROPIC_MODEL=sonnet
