# Log — 2026-06-24

A mobile-polish and saved-grid day: made the working area comfortable on phones,
fixed a couple of small chrome details, then added a Cards/List display style for
the saved grid. Newest at the bottom. Each beat: what changed, a short recap, and
a **Why** when there was a real decision.

## README: live site link (`e186103`)

Added `Live at colorfordays.com` under the H1. The repo stays named `palette-forge`
(internal codename); the public brand is "Color for Days". No rename — repo name is
private plumbing; the hosted URL and in-app brand are independent of it.

## Mobile polish: strips, heart, header buttons (`32db9df`)

Changed: `scene-variations.tsx` (1-across strips on mobile, 3 at `sm:`; render only
the first two strips per round on mobile via an nth-child limiter — engine still
composes the full round, so data stays whole; extracted a shared `RoundStrips`),
`palette-card.tsx` (heart 20px mobile / 16px desktop), `index.tsx` (mobile-only
`flex-row-reverse` + wider gap on the working-area header).

**Why:** on a phone the round card crammed three 7-cell strips into ~310px (tiny
swatches, pinched heart), and Re-run sat 8px from the X — easy to fat-finger a
close. Putting Re-run in the screen-edge corner makes the edge a backstop against
overshoot; the destructive X moves inward. Desktop untouched throughout.

## Font dropdown: primary-only trigger (`520b2cd`)

Changed: `global-nav.tsx` — the closed pairing trigger shows just the heading font
(`current.heading ?? 'System'`) instead of the full "X + Y" label, which ran too
long. The open menu still spells out the body font beneath each option.

## Saved grid: Cards/List display style (`bfd4557`)

Changed: new `compact-card.tsx` + `view-mode-toggle.tsx`; `prefs-repo.ts`,
`settings.ts`, `index.tsx`, `preferences.tsx`, `export-modal.tsx`.

A second display style for the saved grid, for users who just want to scan colors.
New persisted `savedView` pref (cards | list, default cards), set from a header
toggle opposite the "+" and mirrored in Settings ("Palette display style"). List
cards render just the `SwatchRow` strip — no label, no flip, no specimen — with
two click behaviours by viewport:

- **Mobile**: tap a row to reveal inline controls (trash · code · light/dark) in a
  single-open accordion (parent-owned open state; tapping another row collapses the
  prior).
- **Desktop** (`>=sm` via `matchMedia`): a click jumps straight to the code popup —
  find-and-share, no inline controls.

`ExportModal` gained a light/dark preview toggle on the format-pills row (far right,
opposite Hex/CSS/Tailwind); it flips the swatch preview while the exported code
still carries both modes.

**Why:** desktop's wall-of-palettes is the fun; on a phone a lighter slice reads
better. But a pure gallery added little on desktop — giving a desktop click the
"open the code to share" job is what makes List useful there. Mobile and desktop
deliberately diverge in click behaviour, not in data. Browser-verified both
viewports; prod `vite build` green throughout.

## AI layer — Phase 0: BYO-key foundation (`feat/ai-layer`)

Started the v2 AI-layer epic ([`docs/epic-ai-layer.md`](../docs/epic-ai-layer.md)) on a
feature branch. Phase 0 is the load-bearing seam everything else builds on: a thin,
local, serverless key layer. With no key the app is byte-for-byte unchanged.

- `prefs-repo.ts` — persist `apiKey` + `model` (IndexedDB `settings` store); new
  `ChatModel = 'haiku' | 'sonnet'` type lives here, the lowest layer.
- `lib/settings.ts` — mirror both into the synchronous in-memory settings so the UI
  can gate on *existence* without an async hop (no flash of AI UI).
- `features/agent/client.ts` — the single seam: `hasKey()` (sync gate) and
  `callAnthropic()` (streaming generator) + `collect()`. The SDK (`@anthropic-ai/sdk`)
  is `await import()`-ed on first call only, so a no-key visitor never downloads it.
  Browser-direct via `dangerouslyAllowBrowser`.
- Settings UI — new "AI touches" section (`ai-access.tsx` container + `key-entry.tsx`
  masked field with show/hide + honest storage note + `model-control.tsx`). Model
  picker only renders once a key is present.

**Why the SDK over hand-rolled fetch (reversal of my first instinct):** the epic ends
at streaming (Phase 3) and structured tool-use + multi-turn (Phase 4) — exactly where
the SDK earns its keep (typed content blocks, partial-JSON tool accumulation). The only
objection was bundle weight on a static app, and a lazy import behind `hasKey()`
dissolves it: no-key users pay zero bytes (client.ts has no consumer yet, so it's
tree-shaken entirely; once Phase 1 consumes it the dynamic import keeps it split).

Gate green: `tsc`, `eslint`, `pnpm build`. Browser-verified Settings renders the AI
section with no key and the honest note; home page untouched.

## AI layer — Phase 1: Rename (manual + AI suggestions) (`feat/ai-layer`)

Phase 1 landed as a **rename dialog**, not the spec's silent per-card swap. The
design grew through review into something better: rename is a base feature for
everyone, and AI is *lightly offered* inside it — never forced, never silent.

- **Rename is for everyone (pencil icon, "Rename this palette" tooltip).** Clicking
  opens a dialog that follows the delete/export convention — the palette's own
  `SwatchRow` on top. Manual edit (a text field + Save) always works and needs no
  key. This is a non-AI base feature, so the rename affordance is *not* key-gated.
- **AI suggestions are the gated add-on.** A "or let AI suggest" section appears in
  the dialog only when a key is set (no nag otherwise). "Suggest names" calls Haiku
  for 4 distinct two-word names; pick one to apply, or Regenerate. Loading and error
  states are *visible* — a failed call shows "Couldn't reach Anthropic. Check your
  API key in Settings." + Try again, instead of failing invisibly.
- `features/agent/rename.ts` — `suggestNames(palette, count=4)` calls Haiku with the
  ground/accent hexes + the `knowledge/naming.md` persona; **throws** on API error
  (so the dialog surfaces it) and logs to console. Pure, exported `parseModelName()`
  (one name) and `parseNameList()` (multi-line, strips list markers, dedupes, caps)
  are unit-tested in `rename.test.ts` (12 cases).
- **Remove-key in Settings.** A two-step "Remove key" confirm that *truly* deletes
  the IndexedDB record (`removeApiKey` → `idbDelete`, not a blank write) — verified
  the record reads back `undefined` after removal. Model picker + control vanish.
- `IconButton` gained an optional `title` for the hover tooltip (defaults to `label`).

**Why the redesign (reversal of the spec's silent swap):** a one-shot swap gave the
user no feedback and swallowed every error — a real misconfiguration was invisible.
A dialog makes the operation legible (loading/results/error) and lets the user *not*
use AI (manual edit) — "lightly expose, don't force AI."

**Reinterprets the prime directive:** manual rename is a base feature for all users,
so the no-key app is no longer strictly byte-for-byte — but the **AI** layer stays
absent without a key (no suggestions, no nag). The intent (AI is invisible until you
opt in) holds; the letter ("byte-for-byte") is loosened for one non-AI feature, by
explicit decision.

**Deviation from the plan (followed the code):** the plan named `square-card.tsx`
(presentational, no name); rename lives on the saved cards (`favorite-card`,
`compact-card`) and persists to the addressable record. **Locale deferred:** IP geo
adds a third-party dependency + privacy surface — holding for an explicit decision.

Verified: gate green (`tsc`, `pnpm build`, 12/12 tests); new/changed files lint-clean.
Browser — no key → pencil rename for all, dialog shows manual edit only (no AI nag),
manual rename persists; with key → "or let AI suggest" + Suggest names; bad key →
visible error + Try again (the silent-failure bug, fixed); Settings remove-key → IDB
record gone.

**Pre-existing debt noted (not touched):** `pnpm lint` is red repo-wide — 5
`no-unnecessary-condition` false-positives on `alive`/mounted-flag + array-index
guards in `index.tsx`, `scene-variations.tsx`, `dominant-color.ts`. Predates this
branch; the proper fix is enabling `noUncheckedIndexedAccess` — left for a separate
focused commit so the AI-layer history stays clean.

## Settings polish + open-source footer (`feat/ai-layer`)

A pass over the AI panel after seeing it live:

- Tightened the key copy — dropped the header sub-description; the note under the
  input is now just "Your key is stored in this browser only and sent only to
  Anthropic."
- "Remove key" became a prominent right-aligned **pill in red** (`#e5484d`,
  outlined; the confirm step's commit button is filled red) — the one destructive
  control on the page, so it earns the color the rest of the chrome avoids.
- **Open-source footer beneath the AI panel:** centered, "All code is open source
  and reviewable on GitHub." The BYO-key promise ("sent only to Anthropic") is only
  as good as its verifiability — pointing at the public repo is the honest move, and
  BYO-key against an open repo is a completely standard pattern.
- Nav gear now toggles Settings ↔ Home (separate commit) for quick there-and-back.

## Docs reconciled to the shipped v1 (`feat/ai-layer`)

Swept the project docs for stale claims now that AI is back, and fixed drift that
predated this branch:

- `CLAUDE.md` — "There is no in-app LLM" → "deterministic core, AI optional at the
  boundary"; the "Settings = only delete-confirm / No BYO-key, no model selector"
  line was flatly false (replaced with the AI-touches panel + an AI-layer section).
- `docs/SPEC.md` — "the in-app LLM was removed / fully local, no key" qualified: the
  generation loop stays deterministic and model-free; the optional BYO-key layer is
  the only thing that leaves the browser.
- `README.md` — biggest drift, and not just AI: it still described the pre-2026-06-23
  app (four takes, WCAG badges, "repairs until the policy passes", "rubric it's scored
  against"). Rewrote to match reality — distinct expressive takes, legibility baked in
  (no enforcement/badges/score), rename (manual + optional AI), AI opt-in at the
  boundary. Renamed the "Two verifiers, no model" section to "Deterministic, not
  enforced."
- Memory: new `ai-layer-v1`; `sim-engine-may-be-enough` + `ai-at-the-boundary` updated
  to point forward (AI re-introduced lightly; MCP/API surface still deferred).

**Why:** the user flagged that local docs must accurately reflect the working app —
stale cruft actively misleads future sessions about what exists.

## Lint cleanup: noUncheckedIndexedAccess (`da4c5f2`)

`pnpm lint` had been red repo-wide from pre-existing `no-unnecessary-condition`
false-positives. Fixed at the durable layer instead of suppressing: enabled
`noUncheckedIndexedAccess` in tsconfig — which made the array-bounds guards in
`dominant-color.ts` / `scene-variations.tsx` legitimate — then made the 50
newly-surfaced accesses explicit across 11 files with `!` + a one-line why each
(regex groups, modulo-in-bounds, non-empty-after-guard, fixed-length arrays). No
behavioral change; each assertion documents an invariant the code already held.
The two `index.tsx` positives were a separate matter — TS narrows the `let alive`
mounted-flag to a constant (cleanup closure is lexically later), so the later
guards read as dead; routed reads through an `isMounted()` accessor, kept
effect-local so a strict-mode remount still resets it. Gate green: tsc 0, eslint
clean, 57/57, build.

**Why:** the false-positives traced to the *absence* of `noUncheckedIndexedAccess`,
so turning it on is the root fix — it also hardens every other array access in the
repo as a bonus, rather than papering over three call sites.

## AI layer — Phase 2: Prompt-to-Palette (`feat/prompt-to-palette`, `ede3169`)

A third input on-ramp next to image and seed color: a key-gated text box. Type a
mood ("warm, mid-century, calm") → one model round-trip returns a seed hex → the
deterministic engine runs all six archetypes from it, unchanged. The model touches
only the input; it never computes a color. New `knowledge/prompt.md` persona is the
steerable seam (joined with `KNOWLEDGE_ORDER` into the system prompt, so editing the
taste prose moves the result). `promptToSeed` in a new `prompt-engine.ts`, with a
pure `parseSeedHex` (tested, 6 cases). Popover gains a third section below the
swatches — absent without a key, mirroring rename; "✦ or describe it" + input +
Forge + spinner/error. Maps to the same color `Source` (text kept in `Source.prompt`
for the deferred mood board), so `startJourney`/engine/journey-store are untouched.
Verified in-browser across no-key / with-key / bad-key paths. Gate green: tsc 0,
eslint clean, 63/63, build.

**Why (deviation from the plan):** the plan said a network error should also fall
back to the neutral seed. Split it instead — *unparseable output* recovers to
neutral (you still get a round), but a *transport/API error* throws so the popover
shows "couldn't reach Anthropic." A silent grey round would let a broken key look
like a bad guess, and surfacing it matches the rename precedent (no silent
failures). The happy path still needs a real key to exercise live (deferred).

## Phase 2 v1.1: prompt brief → focused overlay (`feat/prompt-to-palette`, `a1a490a`)

Trying the phase-2 input on a real brief ("a palette for a Pacific Northwest roofing
company… professional but nature-oriented") exposed the UX: a one-line input crammed
into the source popover, where you can't see your own paragraph. Lifted it out. The
popover keeps the quick picks (image, swatches) and gains a rule + a key-gated "Chat
with AI" button that opens a centered overlay (`prompt-dialog.tsx`): heading, an
auto-growing textarea (the whole brief stays visible), plain Submit, ⌘↵, spinner,
inline error. Built on a new extracted `Modal` shell (`components/ui/modal.tsx`) so
the new overlay adds no copy-pasted dialog chrome — the four existing dialogs fold in
later. The model contract is untouched (one seed hex → engine). Verified across
no-key / with-key / bad-key. Gate green: tsc 0, eslint clean, 63/63, build.

**Why (naming + scope):** dropped the "Forge" button label — plain "Submit", and
"Chat with AI" for the entry (forward-looking toward phase-4 conversation). New
`ui-plain-labels-no-cute` memory: action copy stays literal, brand cuteness out of
buttons. Stayed strictly one-shot — reacting to the result / clarifying questions is
phase 4 (the roofing scenario is now recorded in plan-ai-conversational-refine.md as
its motivation), deliberately not pulled forward.

## Model-direct palettes: contract + system prompt (`feat/prompt-to-palette`, `4b9c5f6`)

A thesis turn. The seed-only prompt flow couldn't honor palette-wide intent ("nothing
girly" → pink accent, every time) because the model picks one hex and the deterministic
engine owns the accent — and a refine loop has nothing to act on. So for the prompt flow,
the model authors the whole palette; the engine stays for swatch/image seeds + as the
fallback. Landed the contract: `knowledge/color-theorist.md` (the system prompt, sent
verbatim — no tool-use, no code assembly; reliable JSON is the prompt-craft problem and
the lever is the markdown) + `prompt-palettes.ts` (`generationSystemPrompt`,
`promptToPalettes`, strict parse/validate) + 10 parser tests + `eval/prompts.md` + the
plan doc `docs/plan-ai-model-direct.md`. Prompt heuristics mined (as *reference*) from
meodai/skill.color-expert + jezweb: character-first range, mood from chroma+lightness,
lightness-separation legibility, 60-30-10, pairing — kept lean (deep theory layers later,
meodai's INDEX-of-references is the blueprint). Wiring (ModelEngine + router + Source
'prompt') is the next slice. Gate green: tsc 0, eslint clean, 73/73, build.

**Why (reverses a stated principle):** "AI never computes color" was the repo's line; for
the prompt flow it's now deliberately false — the model authoring every hex is the only
way to honor negative constraints AND to give the refine loop something to adjust. The
deterministic engine isn't gone; it's one of two generators behind the `PaletteEngine`
seam. Docs/memories reconcile in a later slice.

## Model-direct wiring: prompt journeys author palettes (`feat/prompt-to-palette`, `a2b8a36`)

The model-authored path is now live end to end. A `RoutingEngine` behind the
`PaletteEngine` seam (`get-engine.ts`) sends `source.type === 'prompt'` to a new
`ModelEngine` (calls `promptToPalettes`, wraps each via `finalizePalette`, seeds each
record with its own accent, dedupes names journey-wide) and color/image to the
deterministic engine — two generators, one seam, downstream untouched. `Source` gains a
`'prompt'` type (value = the brief); the prompt dialog stops doing its own model call and
just starts a prompt journey (skeletons → takes or a visible error round). Gated the seed
strip/reinject to image sources only (a prompt journey's takes each carry their own seed;
the old shared-value reinject would flatten them). Removed the superseded seed-only path
(`prompt-engine.ts` + test + `knowledge/prompt.md`) — no dead code. Verified in-browser
with a fake key: brief routes to the model engine, makes a real Anthropic call, the 401
surfaces as an error round; "From your description" + Sparkles marker render. Gate green:
tsc 0, eslint clean, 67/67, build.

**Open:** the happy path needs a real key (the eval briefs in `eval/prompts.md`). The
error-round message is the raw SDK error (informative for dev/eval; could be friendlier
for production). Docs/memory reconciliation (the "AI never computes color" reversal) is
still a pending slice.

## AI flow: instant re-run + finished-feel polish (`feat/prompt-to-palette`, `394752b`, `d994bf1`)

Live eval exposed that AI re-run was a full ~10-30s model call each click — killing the
app's most-loved interaction (smashing regen to instantly build a wall of varied color),
bad enough the user would pull their key. Fixed the model: AI authors the opening round
once (premium); every re-run is an instant, free algorithmic variation *of* that output —
`deriveRound` (`agent/derive.ts`) rotates the whole round-0 colorway by a harmonic offset
(reusing `rotateHue` + `finalizePalette`), and `rerunJourney` branches prompt→derive
(synchronous, no wait, no tokens) / color·image→deterministic. Round 0 honors intent;
rotations are the playground (`394752b`, 3 tests).

Then three finished-feel fixes (`d994bf1`): the running round's skeletons were invisible
on dark — now a spinner + the live `journey.progress` label over bordered skeletons; the
header quotes the verbatim brief instead of a generic label; and each palette's
`character` (model rationale / archetype line) shows as a strip hover-title (wall stays
clean) + a saved-card subtitle. Gate green throughout: tsc 0, eslint clean, 70/70, build.

**Still open (this arc):** the model's friendly top-level message (a `{message, palettes}`
contract + the compose seam returning it) and refresh recovery (persist the brief + round-0
so a mid-generation reload isn't a total loss). Full smash-regen + loading visuals need a
real key to see live.
